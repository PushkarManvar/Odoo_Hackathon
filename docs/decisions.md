# Decisions

Locked decisions. Each entry: ID, date, what, why. Append, don't edit history.

| ID | Date | Decision | Rationale |
|----|------|----------|-----------|
| DEC-001 | *TBD* | Multi-AI team PR/merge workflow | Keep all contributors (human + AI) following the same rules; main is protected |
| DEC-002 | 2026-08-21 | Stack is undecided until project idea is finalized | No application code or stack assumptions before kickoff; prevents scaffolding on a never-chosen stack |
| DEC-003 | 2026-08-22 | Local PostgreSQL as the database | Evaluator expects a local database like Postgres; PostgreSQL 17 running locally (port 5432), Supabase MCP still connected as fallback |

*Add new decisions as they are made.*
